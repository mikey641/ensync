# Gemini CLI context

Use `.relay/project.md` and `.relay/architecture.md` as the canonical shared context. Read only relevant files from `.relay/features/`.

Do not build a separate Gemini-specific memory. All coding agents must share the same project and feature decisions.
