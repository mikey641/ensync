# Gemini CLI context

Use `.ensync/project.md` and `.ensync/architecture.md` as the canonical shared context. Read only relevant files from `.ensync/features/`.

Do not build a separate Gemini-specific memory. All coding agents must share the same project and feature decisions.
