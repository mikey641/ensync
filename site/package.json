{
  "name": "ensync-product-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/validate.mjs",
    "preview": "node scripts/preview.mjs",
    "test": "node --test tests/*.test.mjs && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
